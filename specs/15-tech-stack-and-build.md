# 实现与发布附录（Implementation appendix）

本篇为附录，不参与产品逻辑验收。  
产品行为与规则以 `00` 到 `14` 的规格为准。

---

## 1. 用途

- 给研发同学提供“本地开发与打包”的参考入口。
- 记录构建相关约束，避免把实现细节混入产品规则文档。

---

## 2. 原则

- 技术栈可演进，不作为产品规格的一部分。
- 只要产品行为一致，允许替换框架、组件库或构建工具。

---

## 3. 开发与发布指引

统一以仓库根 `README.md` 的“本地开发 / 发布”章节为准。  
若命令或工具链变化，仅需更新根 README 与工程配置，不应回流污染产品逻辑文档。
